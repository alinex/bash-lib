#!/bin/bash
# Integrated help to be called using -h or --help flag.

echo
echo "Build combined bash-lib"
echo
echo -e "$(yellow)$(bold)Usage: $0$(reset)"
echo
echo "-------------------------------------------------------------------------"
echo
echo "Call it without any parameter to build the combined libraries within the dist"
echo "folder. And use them in other commands."
echo
echo "Also consider reading the complete documentation under:"
echo "$GITLAB//blob/$VERSION/README.md"
echo
