# Downloads

The optimized and compressed files are available as:

- [Complete distribution](bash-lib.tgz) (~35kB archive)

- [Base libraries](base.bash) (single bash script)
- [Info libraries](info.bash) (single bash script)
- [Process libraries](process.bash) (single bash script)
- [PSQL libraries](psql.bash) (single bash script)
- [All libraries](all.bash) (single bash script)

> All of the above files are build from latest master branch.

See under ... what is contained in which library package.
