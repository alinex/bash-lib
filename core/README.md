# Core Folder

This folder contains core modules which are always needed if working with the BashLib because they are often used internally, too. All of them are contained in the `base` and `full` package.

Read more about them in the [Core Section](../doc/README.md#Core) in the API documentation.