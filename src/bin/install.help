#!/bin/bash
# Integrated help to be called using -h or --help flag.

echo
echo "Install necessary tools to use bash-lib"
echo
echo -e "$(yellow)$(bold)Usage: $0$(reset)"
echo
echo "Call it without any parameter to install all necessary tools to work with"
echo "the bash-lib and all it's libraries. This needs root privileges through sudo.
echo
echo "Also consider reading the complete documentation under:"
echo "$GITLAB//blob/$REVISION/README.md"
echo
