# Module Folder

This folder contains a list of different modules which are contained in the `full` package, but have be loaded individually if using the `base` package.

Read more about them in the [Module Section](../doc/README.md#Module) in the API documentation.
