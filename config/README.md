# Config Folder

This folder contains configuration files. All of them are bash files which will be loaded into the running script by the [configs](../configs) loader.

Read more about them in the [Config Section](../doc/README.md#Config) in the API documentation.