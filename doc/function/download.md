# download

## Download file

### Usage

```bash
download [<options>] <source> <dest>
```

### Options

```bash
# following options, only before `<source>`
l, login <user>
h, host <host>
j, jump <user>@<host>
```

### Globals

- `$server`
- `$login` - default is "operator"
- 
