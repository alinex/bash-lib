# input

## If stdin and no parameters then read stdin, else use parameters



### Usage

```bash
input "$@ parameters"
```

### Output (stdout)

- `<string-input>` or
- `<string-params>` separated by spaces
