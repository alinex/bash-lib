# timer

## Start/stop the timer

After `timer stop` it will return the elapsed time.


### Usage

```bash
timer start
timer stop
```

### Output (stdout)

- `<seconds>`.`<milliseconds>` # after stop
