test:
  image: node:latest
  script:
    - npm install -g bats
    - bats test
    - test/colors
    - test/log-stderr
    - test/log-auto
    - test/log-file
    - test/log-rotate-size
    - test/log-rotate-file

# lint:
#   image: koalaman/shellcheck-alpine
#   script:
#     - shellcheck include/*.bash
