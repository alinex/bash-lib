test:
  image: node:latest
  script:
    - npm install -g bats
    - bats test
    - test/colors
    - test/log

# lint:
#   image: koalaman/shellcheck-alpine
#   script:
#     - shellcheck include/*.bash
