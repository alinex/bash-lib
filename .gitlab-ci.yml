test:
  image: node:latest
  script:
    - npm install -g bats
    - bats test
    - test/colors
    - test/log-simple
    - test/log-full
    - test/log-auto
    - test/log-file
    - test/log-rotate-size
    - test/log-rotate-time
    - test/log-cmd

# lint:
#   image: koalaman/shellcheck-alpine
#   script:
#     - shellcheck include/*.bash
