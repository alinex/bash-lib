# Extra Folder

The modules within this folders are not contained in the `base` or `full` package and should always be loaded individually if needed.

They contain specialized methods which will only be needed in specific places. So there are organization specific or environment specific modules here.

Read more about them in the [Extra Section](../doc/README.md#Extra) in the API documentation.
