# Locale Folder

This folder contains the localization files:

```bash
locale/
    bashlib.pot         # the base template
    de/LC_MESSAGES/     # the german translation
        bashlib.po      # the source for translation
        bashlib.mo      # the compiled binary
```

Read more about handling this in the [Developer Guide](../doc/develop.md#Internationalization).
